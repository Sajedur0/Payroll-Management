<Window x:Class="PayrollManagement.Views.ReportExportSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="{StaticResource AppFont}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Dialog Header Subtitle -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📄 " FontSize="14"/>
                <TextBlock Text="Configure document layout &amp; paging options" FontSize="12" Foreground="#475569" FontWeight="Medium" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <TextBlock Text="Paper Size:" FontWeight="SemiBold" FontSize="12.5" Foreground="#334155"
                   Grid.Row="1" Grid.Column="0"
                   VerticalAlignment="Center" Margin="0,0,10,14"/>
        <ComboBox ItemsSource="{Binding PaperSizes}"
                  SelectedItem="{Binding PaperSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Grid.Row="1" Grid.Column="1" Margin="0,0,0,14" Padding="8,6"/>

        <TextBlock Text="Orientation:" FontWeight="SemiBold" FontSize="12.5" Foreground="#334155"
                   Grid.Row="2" Grid.Column="0"
                   VerticalAlignment="Center" Margin="0,0,10,16"/>
        <ComboBox ItemsSource="{Binding Orientations}"
                  SelectedItem="{Binding Orientation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Grid.Row="2" Grid.Column="1" Margin="0,0,0,16" Padding="8,6"/>

        <CheckBox Content="Repeat company details on every page"
                  IsChecked="{Binding RepeatCompanyHeader, Mode=TwoWay}"
                  FontSize="12" Foreground="#1E293B"
                  Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                  Margin="0,0,0,10"/>

        <CheckBox Content="Generate separate report per employee"
                  IsChecked="{Binding SeparateReport, Mode=TwoWay}"
                  ToolTip="Individual per-employee report vs. combined report for all employees"
                  FontSize="12" Foreground="#1E293B"
                  Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                  Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2">
            <Button Content="{Binding ActionLabel}" Width="100" Height="36"
                    Background="#4F46E5" Foreground="White" BorderThickness="0"
                    FontWeight="SemiBold" Cursor="Hand" Margin="0,0,10,0"
                    IsDefault="True"
                    Click="Accept_Click"/>
            <Button Content="Cancel" Width="90" Height="36"
                    Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1"
                    Cursor="Hand"
                    IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
