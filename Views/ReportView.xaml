<UserControl x:Class="PayrollManagement.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Soft input style -->
        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Report action buttons base -->
        <Style x:Key="ReportActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.12" Color="#0F172A"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{TemplateBinding Tag}" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.82"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer Padding="20" Background="{StaticResource BackgroundBrush}" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Status & Error Alerts -->
            <Border Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" CornerRadius="8" Padding="14,9" Margin="0,0,0,10">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage}" Value=""><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="ℹ️" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" Foreground="#4338CA" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="8" Padding="14,9" Margin="0,0,0,10">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasError}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⚠️" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="#991B1B" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- FILTERS CARD -->
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.04" Color="#0F172A"/>
                </Border.Effect>
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="#EEF2FF" CornerRadius="8" Width="30" Height="30" Margin="0,0,10,0">
                                <TextBlock Text="◧" Foreground="#4F46E5" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Report Criteria &amp; Filters" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            <Border Background="#F1F5F9" CornerRadius="12" Padding="8,3" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Auto-narrowed dependent dropdowns" FontSize="10.5" Foreground="#64748B"/>
                            </Border>
                        </StackPanel>
                        <Border Background="#FFFBEB" BorderBrush="#FDE68A" BorderThickness="1" CornerRadius="8" Padding="10,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Grace Minutes = Late / Early threshold" FontSize="10.5" Foreground="#92400E" FontWeight="SemiBold"/>
                        </Border>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,14">
                            <TextBlock Text="EMPLOYEE ID" Style="{StaticResource FilterLabelStyle}"/>
                            <TextBox Text="{Binding EmpId, UpdateSourceTrigger=PropertyChanged}" Padding="10,6"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,12,14">
                            <TextBlock Text="SECTION" Style="{StaticResource FilterLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding SectionOptions}" SelectedItem="{Binding Section, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="False" IsSynchronizedWithCurrentItem="False" IsTextSearchEnabled="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,12,14">
                            <TextBlock Text="DESIGNATION" Style="{StaticResource FilterLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding DesignationOptions}" SelectedItem="{Binding Designation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="False" IsSynchronizedWithCurrentItem="False" IsTextSearchEnabled="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,14">
                            <TextBlock Text="CATEGORY" Style="{StaticResource FilterLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding CategoryOptions}" SelectedItem="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="False" IsSynchronizedWithCurrentItem="False" IsTextSearchEnabled="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                            <TextBlock Text="SHIFT" Style="{StaticResource FilterLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding ShiftOptions}" SelectedItem="{Binding Shift, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="False" IsSynchronizedWithCurrentItem="False" IsTextSearchEnabled="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Text="GRACE MINUTES" Style="{StaticResource FilterLabelStyle}"/>
                            <TextBox Text="{Binding GraceMinutes, UpdateSourceTrigger=PropertyChanged}" Padding="10,6"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,12,0">
                            <TextBlock Text="FROM DATE" Style="{StaticResource FilterLabelStyle}"/>
                            <DatePicker SelectedDate="{Binding FromDate}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,0,0">
                            <TextBlock Text="TO DATE" Style="{StaticResource FilterLabelStyle}"/>
                            <DatePicker SelectedDate="{Binding ToDate}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- REPORT GENERATION BUTTONS CARD -->
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.04" Color="#0F172A"/>
                </Border.Effect>
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="#EEF2FF" CornerRadius="8" Width="30" Height="30" Margin="0,0,10,0">
                                <TextBlock Text="⚡" Foreground="#4F46E5" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Generate Reports" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="— Click any report below to execute instantly" FontSize="11.5" Foreground="#94A3B8" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                        <Border Background="#F1F5F9" CornerRadius="8" Padding="10,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Standardized enterprise reporting" FontSize="10.5" Foreground="#64748B" FontWeight="SemiBold"/>
                        </Border>
                    </Grid>
                    <WrapPanel>
                        <Button Content="Attendance Report" Tag="◉" Command="{Binding AttendanceCommand}" Style="{StaticResource ReportActionButton}" Background="#4F46E5"/>
                        <Button Content="Absent Report" Tag="✕" Command="{Binding AbsentCommand}" Style="{StaticResource ReportActionButton}" Background="#DC2626"/>
                        <Button Content="Present Report" Tag="✓" Command="{Binding PresentCommand}" Style="{StaticResource ReportActionButton}" Background="#059669"/>
                        <Button Content="Late Report" Tag="◷" Command="{Binding LateCommand}" Style="{StaticResource ReportActionButton}" Background="#D97706"/>
                        <Button Content="Early Report" Tag="◐" Command="{Binding EarlyCommand}" Style="{StaticResource ReportActionButton}" Background="#7C3AED"/>
                        <Button Content="Duty Duration" Tag="◔" Command="{Binding DutyDurationCommand}" Style="{StaticResource ReportActionButton}" Background="#0284C7"/>
                        <Button Content="Monthly Summary" Tag="▦" Command="{Binding MonthlySummaryCommand}" Style="{StaticResource ReportActionButton}" Background="#0F172A"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- RESULTS TABLE CARD -->
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="0" Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.04" Color="#0F172A"/>
                </Border.Effect>
                <StackPanel>
                    <Border Background="#F8FAFC" CornerRadius="12,12,0,0" Padding="16,14" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="#4F46E5" CornerRadius="8" Width="32" Height="32" Margin="0,0,10,0">
                                    <TextBlock Text="▤" Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ReportTitle}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="Generated data preview and export controls" FontSize="11" Foreground="#64748B" Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Border Background="#EEF2FF" CornerRadius="20" Padding="12,6" Margin="0,0,10,0" VerticalAlignment="Center">
                                    <TextBlock FontSize="11.5" Foreground="#4F46E5" FontWeight="SemiBold">
                                        <Run Text="{Binding Result.Rows.Count, Mode=OneWay}"/><Run Text=" rows generated"/>
                                    </TextBlock>
                                </Border>
                                <Button Content="📄 Export PDF" Command="{Binding ExportCommand}" Padding="14,7" Background="#4F46E5" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="12" FontWeight="SemiBold"/>
                                <Button Content="🖨️ Print" Command="{Binding PrintCommand}" Margin="8,0,0,0" Padding="14,7" Background="#1E293B" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Loading State Banner -->
                    <Grid>
                        <Border Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" CornerRadius="8" Padding="12" Margin="16,14,16,0" HorizontalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="⏳" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Generating report from database, please wait..." FontSize="12" Foreground="#4338CA" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- DataGrid -->
                    <Border Padding="14">
                        <DataGrid ItemsSource="{Binding ResultTable}" AutoGenerateColumns="True" IsReadOnly="True" CanUserAddRows="False" MaxHeight="540"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9" BorderThickness="1" BorderBrush="#E2E8F0" HeadersVisibility="Column" RowHeight="34" AlternatingRowBackground="#FAFAFA" FontSize="12">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                    <Setter Property="Foreground" Value="#475569"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="10,8"/>
                                    <Setter Property="FontSize" Value="11.5"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F1F5F9"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#EEF2FF"/>
                                            <Setter Property="Foreground" Value="#1E1B4B"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
